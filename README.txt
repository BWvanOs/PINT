git clone https://github.com/BWvanOs/PINT.git
cd PINT
onda env create -f environment.yml
conda activate pint

##This installs all required Python packages and system-level dependencies (e.g. libxml2, libxslt).


